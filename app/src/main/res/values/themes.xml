<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base light/dark theme for the app -->
    <style name="Theme.Markor" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Optional features removed to fix resource linking -->
        <!-- Remove until dependencies are confirmed -->
        <!-- <item name="preferenceCategoryStyle">@style/Preference.Category.Material3</item> -->
        <!-- <item name="preferenceTheme">@style/PreferenceThemeOverlay.Material3</item> -->

        <!-- Android 14+ only; causes error if targetSdk < 34 -->
        <!-- <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item> -->
    </style>

</resources>
